export default function checkEnvironment(): string {
  const envUrl = '';
  // const envUrl =
  //   process.env.NODE_ENV === 'development' ? 'http://localhost:3000' : window.location.hostname;

  return envUrl;
}
